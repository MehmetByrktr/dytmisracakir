import { ArrowRight } from 'lucide-react';
import Button from '@/components/ui/Button';
import RevealOnScroll from '@/components/ui/RevealOnScroll';
import { site as defaultSite } from '@/data/site';
export default function AppointmentCTA({ siteData = defaultSite }: { siteData?: typeof defaultSite }) { const text = siteData.texts.home; return <section className="section-pad"><div className="container-site"><RevealOnScroll><div className="relative overflow-hidden rounded-xl2 border border-ink/[0.06] bg-gradient-to-br from-sage-50 via-cream to-clay-soft/25 px-8 py-16 text-center sm:px-16"><div className="rota-line pointer-events-none absolute inset-x-10 top-10 h-px opacity-60" aria-hidden /><span className="eyebrow">{text.ctaEyebrow}</span><h2 className="mx-auto mt-4 max-w-xl text-balance font-display text-3xl font-medium leading-tight text-ink sm:text-4xl">{text.ctaTitle}</h2><p className="mx-auto mt-4 max-w-md text-sm leading-relaxed text-ink-soft">{text.ctaDescription}</p><div className="mt-8 flex justify-center"><div className="animate-pulse-soft rounded-full motion-reduce:animate-none"><Button href="/randevu" size="lg" icon={<ArrowRight className="h-4 w-4" />}>{text.ctaButton}</Button></div></div></div></RevealOnScroll></div></section>; }
