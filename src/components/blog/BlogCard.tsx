import Link from 'next/link';
import Image from 'next/image';
import { Clock, ArrowUpRight } from 'lucide-react';
import type { BlogPost } from '@/types';
import { formatDate } from '@/lib/utils';
export default function BlogCard({ post }: { post: BlogPost }) { return <Link href={`/blog/${post.slug}`} className="paper-card group flex h-full flex-col overflow-hidden !border-ink/20 !bg-[#F7F3ED] shadow-card transition-all duration-300 hover:-translate-y-1.5 hover:!border-ink/35 hover:shadow-card-hover"><div className="relative flex aspect-[16/10] items-center justify-center overflow-hidden bg-sage-50">{post.coverImage ? <Image src={post.coverImage} alt={post.title} fill sizes="(max-width: 640px) 100vw, 33vw" className="object-cover transition duration-500 group-hover:scale-[1.04]" /> : <span className="px-6 text-center font-mono text-[11px] uppercase tracking-wide text-sage-600">Kapak görseli</span>}<span className="absolute left-4 top-4 rounded-full bg-cream/95 px-3 py-1 font-mono text-[11px] uppercase tracking-wide text-clay-deep shadow-sm">{post.category}</span></div><div className="flex flex-1 flex-col p-6"><h3 className="font-display text-lg leading-snug text-illusion transition-colors group-hover:text-illusion-deep">{post.title}</h3><p className="mt-2 flex-1 text-sm leading-relaxed text-ink-soft">{post.excerpt}</p><div className="mt-5 flex items-center justify-between text-xs text-ink-faint"><span>{formatDate(post.publishedAt)}</span><span className="flex items-center gap-1"><Clock className="h-3.5 w-3.5" /> {post.readMinutes} dk okuma</span></div><span className="mt-4 inline-flex items-center gap-1.5 text-sm font-semibold text-clay-deep">Devamını Oku<ArrowUpRight className="h-4 w-4 transition-transform duration-300 group-hover:translate-x-1 group-hover:-translate-y-1" /></span></div></Link>; }
